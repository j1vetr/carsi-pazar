rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Tüm istemci erişimi kapalı; sadece Cloud Functions (admin SDK) yazar/okur.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
